type User {
  id: ID! @id,
  name: String!,
  age: Int!,
  location: String!,
  password: String,
  cars: [Car]
}

type Car {
  id: ID! @id,
  owner: User!,
  model: String!,
  brand: String!,
  year: Int!,
  horsepower: Int!
}
